/**
 * Header icon chip on dashboard cards — same soft treatment as each row in
 * `ActivityItem` (rounded-xl bg-white/[0.04] text-zinc-300), scaled for size-11.
 */
export const dashboardStatIconShell =
  "flex size-11 shrink-0 items-center justify-center rounded-2xl bg-white/[0.04] text-zinc-300"
