/** Persistent storage root on the host (outside the application git clone). */
export const ARCIIN_DEFAULT_STORAGE_ROOT = "/srv/arciin-storage/arciin"

/** In-repo path for local development only — not the production default. */
export const ARCIIN_DEV_STORAGE_ROOT = "./data/arciin"
